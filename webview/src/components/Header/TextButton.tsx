import React from 'react';
import { DefaultButton } from '@fluentui/react';

const TextButton: React.FC = () => {
  const handleClick = () => {
    // TODO: Implement sketch functionality
  };

  return <DefaultButton text="Text" onClick={handleClick} />;
};

export default TextButton;
